spring:
  data:
    mongodb:
      host: localhost
      database: demo
      username: root
      password: password
      authentication-database: admin

logging:
  level:
    org:
      springframework:
        data:
          mongodb:
            core:
              MongoTemplate: DEBUG