// map, filter, reduce, every, some

// quick boolean check, whether it satisfy the condition you want to check
const numbers = [1,2,3,4,5];

const isAllEven = numbers.every((number) => number > 3);

console.log(isAllEven);

console.log('<----------------->');

const isGreaterThanZero = numbers.every((number) => number > 0);

console.log(isGreaterThanZero);

export { };

